#include<stdio.h>
#include<conio.h>
int
main ()
{
  float radius, Area;
             printf ("enter the redius of circule\n ");
                scanf ("%f", &radius);
                    Area = 3.14 * radius * radius;
	printf ("the Radius of circul is = %f \n and the area is %f", radius, Area);
                        
                        
  return 0;
}

